image:
  file: .gitpod.Dockerfile

tasks:
  - init: dotnet --info
  - command: dotnet restore && dotnet build
  - command: dotnet test

vscode:
  extensions:
    - ms-dotnettools.csharp

ports:
  - port: 5000
    onOpen: open-preview
  - port: 5001
    onOpen: ignore
